#include <iostream>
using namespace std;

class Shape{
    public:
    void draw(); // pure virtual function
};



int main(){
    // hiding all unnecessary details and showing only the important parts
    // abstract class and pure virtual function
    
    /*
    Properties of abstract function- 

    1. abstract classes are used to provide a base class from which other classes can be derived
    2. they cannot be instantiated and are mean to be inherited || does not make their objects
    3. abstract classes are typically used to define an interface for   derived class 
    4. have atleast one pure virtual function
    */

}