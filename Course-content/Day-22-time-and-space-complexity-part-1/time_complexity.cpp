#include <iostream>
using namespace std;

int main()
{
    // the amount of time taken up by an algorithm as a function of input size(n)

    // types of time complexity
    // 1. constant time complexity - O(1)
    // 2. O(n2) quadratic TC
    // 3. O(n) linear TC
    // 4. O(logn) logarithmic TC
    // 4. O(2^n) exponential TC

    // if time complexity is given in the function like quadratic in n or any other ... then the time complexity is the largest one

    // n^2 + n + 1 = timecomplexity(n^2)ans..

    // other notations of time complexity
    // 1. big omega - best time complexity
    // 2. big theta - average time complexity
    return 0;
}